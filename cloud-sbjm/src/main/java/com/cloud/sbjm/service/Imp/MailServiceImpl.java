package com.cloud.sbjm.service.Imp;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.beans.factory.annotation.Value;import org.springframework.mail.SimpleMailMessage;import org.springframework.mail.javamail.JavaMailSender;import org.springframework.stereotype.Service;import com.cloud.sbjm.common.Constant;import com.cloud.sbjm.onput.MailRequest;import com.cloud.sbjm.onput.MailResponse;import com.cloud.sbjm.service.MailService;/** * @author ouyangzhicheng * @version V1.0 * @Title: MailService.java * @Description: 邮件处理接口实现类 * @date 2018年2月6日 */@Servicepublic class MailServiceImpl implements MailService {    //Spring的邮件工具类，实现了MailSender和JavaMailSender接口    @Autowired    private JavaMailSender mailSender;    @Value("${spring.mail.username}")    private String Sender_username;    @Override    public MailResponse sendMail(MailRequest request) {        MailResponse response = new MailResponse();        if (request.getAddressee() == null || request.getAddressee().equals("")) {            response.setCode(Constant.EMAIL_ADDRESS_IS_NULL_CODE);            response.setMsg(Constant.EMAIL_ADDRESS_IS_NULL__MSG);            return response;        }        System.out.println("邮件开始发送.....");        SimpleMailMessage message = new SimpleMailMessage();        message.setFrom(Sender_username);        message.setTo(request.getAddressee());        message.setSubject(request.getSubject());        message.setText(request.getTextValue());        mailSender.send(message);        System.out.println("邮件发送完成！");        response.setCode(Constant.SUCCESS_CODE);        response.setCode(Constant.SUCCESS_MSG);        return response;    }}